language: c

compiler:
  - gcc

before_deploy:
  - make PREFIX=/opt/id-generator install

deploy:
  provider: releases
  api_key: $CI_USER_TOKEN
  skip_cleanup: true
  file:
    - /opt/id-generator
  on:
    tags: true
    all_branches: true